#include<iostream>
using namespace std;

const int N=1e5+10;
int m,p;

struct SegTreeNode
{
    int l;
    int r;
    int v;
}sgt[N];


void build(int l,int r,int idx){
    sgt[idx]
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    cin>>m>>p;
    build(1,m,1);
    
    for()
}