#include<bits/stdc++.h>
using namespace std;

#define endl '\n'

const int N=2e5+10;
int num[N];



int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int t,n,k;
    cin>>t;
    while(t--){
        cin>>n>>k;
        for(int i=1;i<=n;i++) cin>>num[i];
        sort(num+1,num+1+n);
        for(int i=1;i<=k;i++) num[]
    }
}