#include<iostream>
using namespace std;

typedef long long ll;
double prefix;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    int n;
    double g;
    cin>>n>>g;
    for(int i=1;i<=n;i++){
        double in;
        cin>>in;
        prefix+=in;
        
    }
}