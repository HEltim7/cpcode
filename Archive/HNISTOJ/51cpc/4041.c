#include<stdio.h>
int main()
{
    int lenth,step=0;
    char str[1024]={' '};
    scanf("%d",&lenth);
    gets(str);
    for (int i = lenth-1; i > 0; i--)
    {
        for (int j = 0; j < i; j++)
        {
            if (str[j+1])
            {
                
            }
            
        }
        
    }
    
}