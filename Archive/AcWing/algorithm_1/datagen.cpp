#include <bits/stdc++.h>
using namespace std;

#define endl '\n'
#define debug(x) cout << "[debug] " #x << " = " << x << endl

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);
    freopen("data.txt","w+",stdout);
    int n=2e5+10;
    cout<<100000<<' '<<200000<<endl;
    for(int i=1;i<=n;i++){
        cout<<i<<' '<<i<<' '<<i<<' ';
    }
    return 0;
}