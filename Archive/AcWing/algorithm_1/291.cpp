#include<iostream>
using namespace std;

typedef long long ll;
const int N=12,M=1<<N;

int n,m;// n行m列
int dp[N][M];

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0),cout.tie(0);
    while(cin>>n>>m && n||m){
        
    }
}