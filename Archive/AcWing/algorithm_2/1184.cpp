#include<vector>
#include<iostream>
#include<algorithm>
using namespace std;

#define endl '\n'
using LL=long long;
const int N=1e5+10;
vector<int> ans;
vector<int> adj[N];

void hierholzer(int u) {
    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    
}