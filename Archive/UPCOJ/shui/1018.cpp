#include<bits/stdc++.h>
using namespace std;

#define endl '\n';
const int N=1e5+10;

int main(){
    std::ios::sync_with_stdio(0);
    std::cin.tie(0),std::cout.tie(0);
    double n,m;
    while(cin>>n>>m) cout<<(long long)(n*m*38)/100*100<<endl;
    return 0;
}