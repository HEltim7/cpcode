#include<vector>
#include<iostream>
#include<algorithm>
#include<iomanip>
#include<cmath>
using namespace std;

#define endl '\n'
using LL=long long;

void solve() {
    double a,b;
    int d;
    cin>>a>>b>>d;
    if(d==180) cout<<-a<<' '<<-b;
    else {
        // double alpha;
        // // if(b==0) alpha=
        // alpha=atan(a/b);
    }
    cout<<PI;
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    solve();
    return 0;
}